en la sesión de hoy se ha trabajado la herencia.

Se ha creado la clase Forma y las subclases Cuadrado, Rectángulo y Triángulo y Círculo.

Se ha comentado la visibilidad de los atributos y métodos.

Private (solo en clase)
Public (en cualquier clase)
Protected (en la clase y en las subclases)
package (en la clase y en el mismo paquete)

y como se diferencia la teoria de la práctica con Java ya que:

private: solo en la clase
public: en cualquier clase
protected: es equivalente a package

why java why?!?!

Se ha trabajado el polimorfismo vertical:

la clase hereda y sobreescribe los métodos de la clase padre.

Y el polimorfismo horizontal:

la clase sobrecarga los metodos con el mismo nombre pero con diferentes parámetros.

(sobrecarga: mismo nombre, diferente firma)
